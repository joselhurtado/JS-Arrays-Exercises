// Need help on this

let myArray = [42, true, "towel", [2,1], 'hello', 34.4, {"name": "juan"}];

let hello = [];
for(let i = 0; i < myArray.length; i++ ){
    myArray.length = myArray.length -0;
    if(typeof variable === {});
    hello.push(typeof(myArray))
}

console.log(hello)
